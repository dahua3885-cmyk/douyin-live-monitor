# 更新记录

## 0.4.3 — 2026-09-08

- 结构拆解自动加载保存结果，直播中定期更新草稿；内容不变复用缓存，移除每次手动生成的操作。
- 缓存识别同长度文本修改和媒体状态变化，显示更新时间与待转写状态，导出复用缓存。

- 完成且验证可用的 MP4 成品目录只展示 MP4；对应原文件和程序记录经校验后转存内部目录，失败或仍在录制的内容保留原位。
- 打开录像文件夹时恢复并激活对应资源管理器窗口；只切到前台一次，不永久置顶。

- 账号录像开关下方和历史录像旁增加「打开位置」，直接定位实际视频所在文件夹；全局设置提供「打开保存目录」。

- 单个录像文件时长与每场累计上限默认均为不限时；不分段模式写入一个 TS，结束后归档并可转成 MP4。
- 文件夹浏览改为页面内磁盘/目录选择，不再等待外部 Windows 选择窗口。

- 首次从账号录像开关进入完整全局设置；目录选择先暂存，点击「保存并开启录像」才启用，取消不启动录像。

- 全局录像设置收进弹窗，从直播间列表工具栏的「录像设置」打开；主页面不再展开整块设置。
- 支持取消、Esc 关闭并放弃未保存参数；保存成功后关闭。保持全局设置和账号独立录像开关。

## 0.4.2 — 首次公开源码

- 多直播间监控、批量导入和批量操作。
- 全局录像设置：保存目录、画质回退、分段、累计上限及 MP4 转换；账号单独选择是否录像。
- 本地音频转写、持久保存、导出、直播档案、话术搜索和媒体定位。
- 基于原话和时间的规则结构草稿、重复轮次提示。
- 页面异常提醒和可选飞书群机器人通知。
- Windows 后台守护、登录后自启和停止入口。
- 通用安装说明、离线模型下载入口、独立数据目录、源码许可和自动化测试工作流。
